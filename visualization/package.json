{
  "name": "visualizer",
  "version": "1.0.0",
  "description": "Runs the local HTML provinding interaction with the user",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0"
  }
}
